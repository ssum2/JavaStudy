public class Member2 
{
	String userid;  // 아이디 (문자열)
	String pwd;     // 암호  (문자열)
	String name;    // 성명  (문자열)
	int age;        // 나이  (정수)
	String address; // 주소  (문자열)

    public Member2() { } // 기본생성자 생략
	// === 기본생성자 ===
	// 기본생성자는 파라미터가 있는 생성자가 없을 경우 자동으로 등록되어지지만
	// 그러나 파라미터가 있는 생성자가 있을 경우 자동으로 등록이 안되어진다.
	// 그러므로 파라미터가 있는 생성자가 있을 경우에는 반드시 기본생성자를 생성해주어야 한다.!!!!!

    public Member2(String userid, String pwd, String name, int age, String address) {
        this.userid = userid; 
		this.pwd = pwd;
		this.name = name;
		this.age = age;
		this.address = address;
	}

	public void showInfo() {
		System.out.println("====================");
        System.out.println("1.아이디 : " + userid);
        System.out.println("2.암 호 : " + pwd);
        System.out.println("3.성 명 : " + name);
        System.out.println("4.나 이 : " + age);
        System.out.println("5.주 소 : " + address);
		System.out.println("====================");
	}
}
